/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */


import ApiClient from "../ApiClient";
import AgencyPolicies from '../model/AgencyPolicies';
import AgencyQuotes from '../model/AgencyQuotes';
import GetAgencyAgents200Response from '../model/GetAgencyAgents200Response';
import GetAgencyAgents401Response from '../model/GetAgencyAgents401Response';
import GetAgencyAgents404Response from '../model/GetAgencyAgents404Response';
import GetAgent200Response from '../model/GetAgent200Response';

/**
* Agency service.
* @module api/AgencyApi
* @version 3.0.0
*/
export default class AgencyApi {

    /**
    * Constructs a new AgencyApi. 
    * @alias module:api/AgencyApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instance} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }



    /**
     * Get agency agents
     * Retrieves a list of agents associated with your agency. Optionally, you can filter using email and/or national producer number within the query parameters.
     * @param {Object} opts Optional parameters
     * @param {String} [email] The email address of the agent
     * @param {String} [nationalProducerNumber] The national producer number of the agent
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/GetAgencyAgents200Response} and HTTP response
     */
    getAgencyAgentsWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'email': opts['email'],
        'national_producer_number': opts['nationalProducerNumber']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['agency_access_token', 'legacy_api_key'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = GetAgencyAgents200Response;
      return this.apiClient.callApi(
        '/v3/agency/agents', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get agency agents
     * Retrieves a list of agents associated with your agency. Optionally, you can filter using email and/or national producer number within the query parameters.
     * @param {Object} opts Optional parameters
     * @param {String} opts.email The email address of the agent
     * @param {String} opts.nationalProducerNumber The national producer number of the agent
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/GetAgencyAgents200Response}
     */
    getAgencyAgents(opts) {
      return this.getAgencyAgentsWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get policies for your agency
     * Retrieves a list of policies associated with your agency. Optionally, you can provide parameters to limit and/or filter your query.
     * @param {Object} opts Optional parameters
     * @param {Boolean} [active] Whether or not the found policies will be active.
     * @param {String} [agentId] The agent id the found policies will have.
     * @param {String} [policyholderEmail] The policyholder email to search for. Uses exact matching.
     * @param {String} [policyNumber] The policy number to search for. Uses exact matching.
     * @param {String} [firstName] The first name of the primary named insured. Uses fuzzy matching. Both first_name and last_name are required when searching by name.
     * @param {String} [lastName] The last name of the primary named insured. Uses fuzzy matching. Both first_name and last_name are required when searching by name.
     * @param {Number} [limit] The maximum number of policies to return. This defaults to unlimited.
     * @param {Number} [offset] The number of policies to offset before returning a number of policies up to the passed limit. This defaults to 0 and the offset is by created_at descending.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/AgencyPolicies} and HTTP response
     */
    getAgencyPoliciesWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'active': opts['active'],
        'agent_id': opts['agentId'],
        'policyholder_email': opts['policyholderEmail'],
        'policy_number': opts['policyNumber'],
        'first_name': opts['firstName'],
        'last_name': opts['lastName'],
        'limit': opts['limit'],
        'offset': opts['offset']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['agency_access_token', 'legacy_api_key'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = AgencyPolicies;
      return this.apiClient.callApi(
        '/v3/agency/policies', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get policies for your agency
     * Retrieves a list of policies associated with your agency. Optionally, you can provide parameters to limit and/or filter your query.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.active Whether or not the found policies will be active.
     * @param {String} opts.agentId The agent id the found policies will have.
     * @param {String} opts.policyholderEmail The policyholder email to search for. Uses exact matching.
     * @param {String} opts.policyNumber The policy number to search for. Uses exact matching.
     * @param {String} opts.firstName The first name of the primary named insured. Uses fuzzy matching. Both first_name and last_name are required when searching by name.
     * @param {String} opts.lastName The last name of the primary named insured. Uses fuzzy matching. Both first_name and last_name are required when searching by name.
     * @param {Number} opts.limit The maximum number of policies to return. This defaults to unlimited.
     * @param {Number} opts.offset The number of policies to offset before returning a number of policies up to the passed limit. This defaults to 0 and the offset is by created_at descending.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/AgencyPolicies}
     */
    getAgencyPolicies(opts) {
      return this.getAgencyPoliciesWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get quotes for your agency
     * Retrieves a list of quotes associated with your agency. Optionally, you can provide parameters to limit and/or filter your query.
     * @param {Object} opts Optional parameters
     * @param {String} [agentId] The agent id associated to the found quotes.
     * @param {Boolean} [bound] Whether or not the found quotes will be bound.
     * @param {Array.<module:model/String>} [quoteReasons] Filter quotes by one or more reasons they may have been created for.
     * @param {Date} [createdAfter] The date after which the found quotes will have been created.
     * @param {Date} [createdBefore] The date before which the found quotes will have been created.
     * @param {String} [email] The email the found quotes will have.
     * @param {String} [firstName] The first name the found quotes will have. This query param has fuzzy find, so you can pass a partial name.
     * @param {String} [lastName] The first name the found quotes will have. This query param has fuzzy find, so you can pass a partial name.
     * @param {Number} [offset] The number of quotes to offset before returning a number of quotes up to the passed limit. This defaults to 0 and the offset is by created_at descending. We recommend you use this in combination with created_before for predictable paging.
     * @param {Number} [limit] The maximum number of quotes to return. This defaults to and caps at 10000.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/AgencyQuotes} and HTTP response
     */
    getAgencyQuotesWithHttpInfo(opts) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
      };
      let queryParams = {
        'agent_id': opts['agentId'],
        'bound': opts['bound'],
        'quote_reasons[]': this.apiClient.buildCollectionParam(opts['quoteReasons'], 'multi'),
        'created_after': opts['createdAfter'],
        'created_before': opts['createdBefore'],
        'email': opts['email'],
        'first_name': opts['firstName'],
        'last_name': opts['lastName'],
        'offset': opts['offset'],
        'limit': opts['limit']
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['agency_access_token', 'legacy_api_key'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = AgencyQuotes;
      return this.apiClient.callApi(
        '/v3/agency/quotes', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get quotes for your agency
     * Retrieves a list of quotes associated with your agency. Optionally, you can provide parameters to limit and/or filter your query.
     * @param {Object} opts Optional parameters
     * @param {String} opts.agentId The agent id associated to the found quotes.
     * @param {Boolean} opts.bound Whether or not the found quotes will be bound.
     * @param {Array.<module:model/String>} opts.quoteReasons Filter quotes by one or more reasons they may have been created for.
     * @param {Date} opts.createdAfter The date after which the found quotes will have been created.
     * @param {Date} opts.createdBefore The date before which the found quotes will have been created.
     * @param {String} opts.email The email the found quotes will have.
     * @param {String} opts.firstName The first name the found quotes will have. This query param has fuzzy find, so you can pass a partial name.
     * @param {String} opts.lastName The first name the found quotes will have. This query param has fuzzy find, so you can pass a partial name.
     * @param {Number} opts.offset The number of quotes to offset before returning a number of quotes up to the passed limit. This defaults to 0 and the offset is by created_at descending. We recommend you use this in combination with created_before for predictable paging.
     * @param {Number} opts.limit The maximum number of quotes to return. This defaults to and caps at 10000.
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/AgencyQuotes}
     */
    getAgencyQuotes(opts) {
      return this.getAgencyQuotesWithHttpInfo(opts)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


    /**
     * Get agent
     * Retrieves an agent by ID.
     * @param {String} agentId 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with an object containing data of type {@link module:model/GetAgent200Response} and HTTP response
     */
    getAgentWithHttpInfo(agentId) {
      let postBody = null;
      // verify the required parameter 'agentId' is set
      if (agentId === undefined || agentId === null) {
        throw new Error("Missing the required parameter 'agentId' when calling getAgent");
      }

      let pathParams = {
        'agent_id': agentId
      };
      let queryParams = {
      };
      let headerParams = {
      };
      let formParams = {
      };

      let authNames = ['agency_access_token', 'legacy_api_key'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = GetAgent200Response;
      return this.apiClient.callApi(
        '/v3/agency/agents/{agent_id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, null
      );
    }

    /**
     * Get agent
     * Retrieves an agent by ID.
     * @param {String} agentId 
     * @return {Promise} a {@link https://www.promisejs.org/|Promise}, with data of type {@link module:model/GetAgent200Response}
     */
    getAgent(agentId) {
      return this.getAgentWithHttpInfo(agentId)
        .then(function(response_and_data) {
          return response_and_data.data;
        });
    }


}
