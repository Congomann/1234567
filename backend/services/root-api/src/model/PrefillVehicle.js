/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PrefillVehicle model module.
 * @module model/PrefillVehicle
 * @version 3.0.0
 */
class PrefillVehicle {
    /**
     * Constructs a new <code>PrefillVehicle</code>.
     * A prefill report found vehicle.
     * @alias module:model/PrefillVehicle
     * @param vin {String} The vehicle identification number.
     */
    constructor(vin) { 
        
        PrefillVehicle.initialize(this, vin);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, vin) { 
        obj['vin'] = vin;
    }

    /**
     * Constructs a <code>PrefillVehicle</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrefillVehicle} obj Optional instance to populate.
     * @return {module:model/PrefillVehicle} The populated <code>PrefillVehicle</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrefillVehicle();

            if (data.hasOwnProperty('make')) {
                obj['make'] = ApiClient.convertToType(data['make'], 'String');
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('vin')) {
                obj['vin'] = ApiClient.convertToType(data['vin'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrefillVehicle</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrefillVehicle</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrefillVehicle.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['make'] && !(typeof data['make'] === 'string' || data['make'] instanceof String)) {
            throw new Error("Expected the field `make` to be a primitive type in the JSON string but got " + data['make']);
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        // ensure the json data is a string
        if (data['vin'] && !(typeof data['vin'] === 'string' || data['vin'] instanceof String)) {
            throw new Error("Expected the field `vin` to be a primitive type in the JSON string but got " + data['vin']);
        }

        return true;
    }


}

PrefillVehicle.RequiredProperties = ["vin"];

/**
 * The vehicle make.
 * @member {String} make
 */
PrefillVehicle.prototype['make'] = undefined;

/**
 * The vehicle model.
 * @member {String} model
 */
PrefillVehicle.prototype['model'] = undefined;

/**
 * The vehicle identification number.
 * @member {String} vin
 */
PrefillVehicle.prototype['vin'] = undefined;

/**
 * The vehicle model year.
 * @member {Number} year
 */
PrefillVehicle.prototype['year'] = undefined;






export default PrefillVehicle;

