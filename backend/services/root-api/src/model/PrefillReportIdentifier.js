/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PrefillReportIdentifier model module.
 * @module model/PrefillReportIdentifier
 * @version 3.0.0
 */
class PrefillReportIdentifier {
    /**
     * Constructs a new <code>PrefillReportIdentifier</code>.
     * A unique identifier for the associated prefill report.
     * @alias module:model/PrefillReportIdentifier
     * @param id {String} The vendor-provided ID from a prefill report.
     * @param source {String} The vendor that provided this prefill report.
     * @param vendorKey {String} The vendor-provided key by which to find the ID from a prefill report.
     */
    constructor(id, source, vendorKey) { 
        
        PrefillReportIdentifier.initialize(this, id, source, vendorKey);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, source, vendorKey) { 
        obj['id'] = id;
        obj['source'] = source;
        obj['vendorKey'] = vendorKey;
    }

    /**
     * Constructs a <code>PrefillReportIdentifier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrefillReportIdentifier} obj Optional instance to populate.
     * @return {module:model/PrefillReportIdentifier} The populated <code>PrefillReportIdentifier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrefillReportIdentifier();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('vendorKey')) {
                obj['vendorKey'] = ApiClient.convertToType(data['vendorKey'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrefillReportIdentifier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrefillReportIdentifier</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrefillReportIdentifier.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['vendorKey'] && !(typeof data['vendorKey'] === 'string' || data['vendorKey'] instanceof String)) {
            throw new Error("Expected the field `vendorKey` to be a primitive type in the JSON string but got " + data['vendorKey']);
        }

        return true;
    }


}

PrefillReportIdentifier.RequiredProperties = ["id", "source", "vendorKey"];

/**
 * The vendor-provided ID from a prefill report.
 * @member {String} id
 */
PrefillReportIdentifier.prototype['id'] = undefined;

/**
 * The vendor that provided this prefill report.
 * @member {String} source
 */
PrefillReportIdentifier.prototype['source'] = undefined;

/**
 * The vendor-provided key by which to find the ID from a prefill report.
 * @member {String} vendorKey
 */
PrefillReportIdentifier.prototype['vendorKey'] = undefined;






export default PrefillReportIdentifier;

