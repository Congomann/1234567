/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DisplayCoverages from './DisplayCoverages';
import Payment from './Payment';

/**
 * The SuggestedCoverages model module.
 * @module model/SuggestedCoverages
 * @version 3.0.0
 */
class SuggestedCoverages {
    /**
     * Constructs a new <code>SuggestedCoverages</code>.
     * A package of suggested coverages. This includes coverage selections and relevant meta data for the set of coverages.
     * @alias module:model/SuggestedCoverages
     * @param coverages {module:model/DisplayCoverages} 
     * @param description {String} A human readable description of this quote.
     * @param derivedFromPriorCoverage {Boolean} Was the customer's current coverages levels factored in generating this quote.
     * @param name {String} A human readable name for this quote.
     */
    constructor(coverages, description, derivedFromPriorCoverage, name) { 
        
        SuggestedCoverages.initialize(this, coverages, description, derivedFromPriorCoverage, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, coverages, description, derivedFromPriorCoverage, name) { 
        obj['coverages'] = coverages;
        obj['description'] = description;
        obj['derivedFromPriorCoverage'] = derivedFromPriorCoverage;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>SuggestedCoverages</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SuggestedCoverages} obj Optional instance to populate.
     * @return {module:model/SuggestedCoverages} The populated <code>SuggestedCoverages</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SuggestedCoverages();

            if (data.hasOwnProperty('coverages')) {
                obj['coverages'] = DisplayCoverages.constructFromObject(data['coverages']);
            }
            if (data.hasOwnProperty('default')) {
                obj['default'] = ApiClient.convertToType(data['default'], 'Boolean');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('derivedFromPriorCoverage')) {
                obj['derivedFromPriorCoverage'] = ApiClient.convertToType(data['derivedFromPriorCoverage'], 'Boolean');
            }
            if (data.hasOwnProperty('fullTermPayment')) {
                obj['fullTermPayment'] = Payment.constructFromObject(data['fullTermPayment']);
            }
            if (data.hasOwnProperty('monthlyTermPayments')) {
                obj['monthlyTermPayments'] = ApiClient.convertToType(data['monthlyTermPayments'], [Payment]);
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SuggestedCoverages</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SuggestedCoverages</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SuggestedCoverages.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `coverages`
        if (data['coverages']) { // data not null
          DisplayCoverages.validateJSON(data['coverages']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // validate the optional field `fullTermPayment`
        if (data['fullTermPayment']) { // data not null
          Payment.validateJSON(data['fullTermPayment']);
        }
        if (data['monthlyTermPayments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['monthlyTermPayments'])) {
                throw new Error("Expected the field `monthlyTermPayments` to be an array in the JSON data but got " + data['monthlyTermPayments']);
            }
            // validate the optional field `monthlyTermPayments` (array)
            for (const item of data['monthlyTermPayments']) {
                Payment.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

SuggestedCoverages.RequiredProperties = ["coverages", "description", "derivedFromPriorCoverage", "name"];

/**
 * @member {module:model/DisplayCoverages} coverages
 */
SuggestedCoverages.prototype['coverages'] = undefined;

/**
 * This coverage package is configured as the default package for your integration
 * @member {Boolean} default
 */
SuggestedCoverages.prototype['default'] = undefined;

/**
 * A human readable description of this quote.
 * @member {String} description
 */
SuggestedCoverages.prototype['description'] = undefined;

/**
 * Was the customer's current coverages levels factored in generating this quote.
 * @member {Boolean} derivedFromPriorCoverage
 */
SuggestedCoverages.prototype['derivedFromPriorCoverage'] = undefined;

/**
 * @member {module:model/Payment} fullTermPayment
 */
SuggestedCoverages.prototype['fullTermPayment'] = undefined;

/**
 * The payments required if the customer pays for the 6-month policy monthly.
 * @member {Array.<module:model/Payment>} monthlyTermPayments
 */
SuggestedCoverages.prototype['monthlyTermPayments'] = undefined;

/**
 * A human readable name for this quote.
 * @member {String} name
 */
SuggestedCoverages.prototype['name'] = undefined;

/**
 * The relative order in which a coverage strategy is recommended, where lower numeric values indicate higher priority.
 * @member {Number} priority
 */
SuggestedCoverages.prototype['priority'] = undefined;






export default SuggestedCoverages;

