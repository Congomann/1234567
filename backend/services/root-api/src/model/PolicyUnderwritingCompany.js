/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PolicyUnderwritingCompany model module.
 * @module model/PolicyUnderwritingCompany
 * @version 3.0.0
 */
class PolicyUnderwritingCompany {
    /**
     * Constructs a new <code>PolicyUnderwritingCompany</code>.
     * The company underwriting this policy.
     * @alias module:model/PolicyUnderwritingCompany
     * @param displayName {String} The human readable name of the underwriting company.
     * @param naic {String} The number that the National Association of Insurance Commissioners (NAIC) assigned to this underwriting company.
     */
    constructor(displayName, naic) { 
        
        PolicyUnderwritingCompany.initialize(this, displayName, naic);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, naic) { 
        obj['displayName'] = displayName;
        obj['naic'] = naic;
    }

    /**
     * Constructs a <code>PolicyUnderwritingCompany</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PolicyUnderwritingCompany} obj Optional instance to populate.
     * @return {module:model/PolicyUnderwritingCompany} The populated <code>PolicyUnderwritingCompany</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PolicyUnderwritingCompany();

            if (data.hasOwnProperty('displayName')) {
                obj['displayName'] = ApiClient.convertToType(data['displayName'], 'String');
            }
            if (data.hasOwnProperty('naic')) {
                obj['naic'] = ApiClient.convertToType(data['naic'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PolicyUnderwritingCompany</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PolicyUnderwritingCompany</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PolicyUnderwritingCompany.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['displayName'] && !(typeof data['displayName'] === 'string' || data['displayName'] instanceof String)) {
            throw new Error("Expected the field `displayName` to be a primitive type in the JSON string but got " + data['displayName']);
        }
        // ensure the json data is a string
        if (data['naic'] && !(typeof data['naic'] === 'string' || data['naic'] instanceof String)) {
            throw new Error("Expected the field `naic` to be a primitive type in the JSON string but got " + data['naic']);
        }

        return true;
    }


}

PolicyUnderwritingCompany.RequiredProperties = ["displayName", "naic"];

/**
 * The human readable name of the underwriting company.
 * @member {String} displayName
 */
PolicyUnderwritingCompany.prototype['displayName'] = undefined;

/**
 * The number that the National Association of Insurance Commissioners (NAIC) assigned to this underwriting company.
 * @member {String} naic
 */
PolicyUnderwritingCompany.prototype['naic'] = undefined;






export default PolicyUnderwritingCompany;

