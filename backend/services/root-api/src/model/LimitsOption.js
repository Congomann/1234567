/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LimitsOption model module.
 * @module model/LimitsOption
 * @version 3.0.0
 */
class LimitsOption {
    /**
     * Constructs a new <code>LimitsOption</code>.
     * An attribute representing the limit levels of this coverage.
     * @alias module:model/LimitsOption
     * @param declined {Boolean} (Experimental) Whether or not these limits are declined. A coverage can have additional limits that can be declined independently. Please see your Root representative to request access to this experimental endpoint.
     * @param description {String} A human readable description of the limit amount(s).
     * @param longDescription {String} (Experimental) A more detailed human readable description of the limit amount(s). Please see your Root representative to request access to this experimental endpoint.
     * @param perDay {Number} The per day limit in USD dollars if applicable.
     * @param perTimePeriod {Number} (Experimental) The per time period limit in USD dollars if applicable, for time periods other than a day (see the description for what the time period actually is). Please see your Root representative to request access to this experimental endpoint.
     * @param perOccurrence {Number} The per occurrence or accident limit in USD dollars if applicable.
     * @param perPerson {Number} The per person limit in USD dollars if applicable.
     */
    constructor(declined, description, longDescription, perDay, perTimePeriod, perOccurrence, perPerson) { 
        
        LimitsOption.initialize(this, declined, description, longDescription, perDay, perTimePeriod, perOccurrence, perPerson);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, declined, description, longDescription, perDay, perTimePeriod, perOccurrence, perPerson) { 
        obj['declined'] = declined;
        obj['description'] = description;
        obj['longDescription'] = longDescription;
        obj['perDay'] = perDay;
        obj['perTimePeriod'] = perTimePeriod;
        obj['perOccurrence'] = perOccurrence;
        obj['perPerson'] = perPerson;
    }

    /**
     * Constructs a <code>LimitsOption</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LimitsOption} obj Optional instance to populate.
     * @return {module:model/LimitsOption} The populated <code>LimitsOption</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LimitsOption();

            if (data.hasOwnProperty('declined')) {
                obj['declined'] = ApiClient.convertToType(data['declined'], 'Boolean');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('longDescription')) {
                obj['longDescription'] = ApiClient.convertToType(data['longDescription'], 'String');
            }
            if (data.hasOwnProperty('perDay')) {
                obj['perDay'] = ApiClient.convertToType(data['perDay'], 'Number');
            }
            if (data.hasOwnProperty('perTimePeriod')) {
                obj['perTimePeriod'] = ApiClient.convertToType(data['perTimePeriod'], 'Number');
            }
            if (data.hasOwnProperty('perOccurrence')) {
                obj['perOccurrence'] = ApiClient.convertToType(data['perOccurrence'], 'Number');
            }
            if (data.hasOwnProperty('perPerson')) {
                obj['perPerson'] = ApiClient.convertToType(data['perPerson'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LimitsOption</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LimitsOption</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LimitsOption.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['longDescription'] && !(typeof data['longDescription'] === 'string' || data['longDescription'] instanceof String)) {
            throw new Error("Expected the field `longDescription` to be a primitive type in the JSON string but got " + data['longDescription']);
        }

        return true;
    }


}

LimitsOption.RequiredProperties = ["declined", "description", "longDescription", "perDay", "perTimePeriod", "perOccurrence", "perPerson"];

/**
 * (Experimental) Whether or not these limits are declined. A coverage can have additional limits that can be declined independently. Please see your Root representative to request access to this experimental endpoint.
 * @member {Boolean} declined
 */
LimitsOption.prototype['declined'] = undefined;

/**
 * A human readable description of the limit amount(s).
 * @member {String} description
 */
LimitsOption.prototype['description'] = undefined;

/**
 * (Experimental) A more detailed human readable description of the limit amount(s). Please see your Root representative to request access to this experimental endpoint.
 * @member {String} longDescription
 */
LimitsOption.prototype['longDescription'] = undefined;

/**
 * The per day limit in USD dollars if applicable.
 * @member {Number} perDay
 */
LimitsOption.prototype['perDay'] = undefined;

/**
 * (Experimental) The per time period limit in USD dollars if applicable, for time periods other than a day (see the description for what the time period actually is). Please see your Root representative to request access to this experimental endpoint.
 * @member {Number} perTimePeriod
 */
LimitsOption.prototype['perTimePeriod'] = undefined;

/**
 * The per occurrence or accident limit in USD dollars if applicable.
 * @member {Number} perOccurrence
 */
LimitsOption.prototype['perOccurrence'] = undefined;

/**
 * The per person limit in USD dollars if applicable.
 * @member {Number} perPerson
 */
LimitsOption.prototype['perPerson'] = undefined;






export default LimitsOption;

