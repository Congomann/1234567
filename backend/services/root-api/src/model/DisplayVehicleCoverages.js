/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VehicleDisplayCoverage from './VehicleDisplayCoverage';

/**
 * The DisplayVehicleCoverages model module.
 * @module model/DisplayVehicleCoverages
 * @version 3.0.0
 */
class DisplayVehicleCoverages {
    /**
     * Constructs a new <code>DisplayVehicleCoverages</code>.
     * This object is meant to represent the current coverage selections associated with a single vehicle.
     * @alias module:model/DisplayVehicleCoverages
     * @param coverages {Array.<module:model/VehicleDisplayCoverage>} The coverages that apply to just this vehicle.
     * @param vin {String} The VIN these coverages apply to.
     */
    constructor(coverages, vin) { 
        
        DisplayVehicleCoverages.initialize(this, coverages, vin);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, coverages, vin) { 
        obj['coverages'] = coverages;
        obj['vin'] = vin;
    }

    /**
     * Constructs a <code>DisplayVehicleCoverages</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DisplayVehicleCoverages} obj Optional instance to populate.
     * @return {module:model/DisplayVehicleCoverages} The populated <code>DisplayVehicleCoverages</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DisplayVehicleCoverages();

            if (data.hasOwnProperty('coverages')) {
                obj['coverages'] = ApiClient.convertToType(data['coverages'], [VehicleDisplayCoverage]);
            }
            if (data.hasOwnProperty('vin')) {
                obj['vin'] = ApiClient.convertToType(data['vin'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DisplayVehicleCoverages</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DisplayVehicleCoverages</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DisplayVehicleCoverages.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['coverages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['coverages'])) {
                throw new Error("Expected the field `coverages` to be an array in the JSON data but got " + data['coverages']);
            }
            // validate the optional field `coverages` (array)
            for (const item of data['coverages']) {
                VehicleDisplayCoverage.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['vin'] && !(typeof data['vin'] === 'string' || data['vin'] instanceof String)) {
            throw new Error("Expected the field `vin` to be a primitive type in the JSON string but got " + data['vin']);
        }

        return true;
    }


}

DisplayVehicleCoverages.RequiredProperties = ["coverages", "vin"];

/**
 * The coverages that apply to just this vehicle.
 * @member {Array.<module:model/VehicleDisplayCoverage>} coverages
 */
DisplayVehicleCoverages.prototype['coverages'] = undefined;

/**
 * The VIN these coverages apply to.
 * @member {String} vin
 */
DisplayVehicleCoverages.prototype['vin'] = undefined;






export default DisplayVehicleCoverages;

