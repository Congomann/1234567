/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AvailableCoverages from './AvailableCoverages';

/**
 * The GetAvailableCoveragesByQuoteId200Response model module.
 * @module model/GetAvailableCoveragesByQuoteId200Response
 * @version 3.0.0
 */
class GetAvailableCoveragesByQuoteId200Response {
    /**
     * Constructs a new <code>GetAvailableCoveragesByQuoteId200Response</code>.
     * The successful response body.
     * @alias module:model/GetAvailableCoveragesByQuoteId200Response
     * @param availableCoverages {Array.<module:model/AvailableCoverages>} The available coverages for the current quote id or market.
     */
    constructor(availableCoverages) { 
        
        GetAvailableCoveragesByQuoteId200Response.initialize(this, availableCoverages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, availableCoverages) { 
        obj['availableCoverages'] = availableCoverages;
    }

    /**
     * Constructs a <code>GetAvailableCoveragesByQuoteId200Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetAvailableCoveragesByQuoteId200Response} obj Optional instance to populate.
     * @return {module:model/GetAvailableCoveragesByQuoteId200Response} The populated <code>GetAvailableCoveragesByQuoteId200Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetAvailableCoveragesByQuoteId200Response();

            if (data.hasOwnProperty('availableCoverages')) {
                obj['availableCoverages'] = ApiClient.convertToType(data['availableCoverages'], [AvailableCoverages]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetAvailableCoveragesByQuoteId200Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetAvailableCoveragesByQuoteId200Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetAvailableCoveragesByQuoteId200Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['availableCoverages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['availableCoverages'])) {
                throw new Error("Expected the field `availableCoverages` to be an array in the JSON data but got " + data['availableCoverages']);
            }
            // validate the optional field `availableCoverages` (array)
            for (const item of data['availableCoverages']) {
                AvailableCoverages.validateJSON(item);
            };
        }

        return true;
    }


}

GetAvailableCoveragesByQuoteId200Response.RequiredProperties = ["availableCoverages"];

/**
 * The available coverages for the current quote id or market.
 * @member {Array.<module:model/AvailableCoverages>} availableCoverages
 */
GetAvailableCoveragesByQuoteId200Response.prototype['availableCoverages'] = undefined;






export default GetAvailableCoveragesByQuoteId200Response;

