/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UnderwritingRequirement model module.
 * @module model/UnderwritingRequirement
 * @version 3.0.0
 */
class UnderwritingRequirement {
    /**
     * Constructs a new <code>UnderwritingRequirement</code>.
     * A requirement that must be met before the policy can be fully underwritten (Experimental)
     * @alias module:model/UnderwritingRequirement
     */
    constructor() { 
        
        UnderwritingRequirement.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UnderwritingRequirement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UnderwritingRequirement} obj Optional instance to populate.
     * @return {module:model/UnderwritingRequirement} The populated <code>UnderwritingRequirement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UnderwritingRequirement();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('actionRequired')) {
                obj['actionRequired'] = ApiClient.convertToType(data['actionRequired'], 'String');
            }
            if (data.hasOwnProperty('retryAt')) {
                obj['retryAt'] = ApiClient.convertToType(data['retryAt'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UnderwritingRequirement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UnderwritingRequirement</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['actionRequired'] && !(typeof data['actionRequired'] === 'string' || data['actionRequired'] instanceof String)) {
            throw new Error("Expected the field `actionRequired` to be a primitive type in the JSON string but got " + data['actionRequired']);
        }

        return true;
    }


}



/**
 * The kind of requirement.
 * @member {module:model/UnderwritingRequirement.TypeEnum} type
 */
UnderwritingRequirement.prototype['type'] = undefined;

/**
 * A human readable description of what is required.
 * @member {String} description
 */
UnderwritingRequirement.prototype['description'] = undefined;

/**
 * The timeframe in which action must be taken in order to satisfy the requirement.
 * @member {module:model/UnderwritingRequirement.ActionRequiredEnum} actionRequired
 */
UnderwritingRequirement.prototype['actionRequired'] = undefined;

/**
 * The date and time at which the quote could be retried and potentially receive an updated status.
 * @member {Date} retryAt
 */
UnderwritingRequirement.prototype['retryAt'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
UnderwritingRequirement['TypeEnum'] = {

    /**
     * value: "ineligible"
     * @const
     */
    "ineligible": "ineligible",

    /**
     * value: "manual_review"
     * @const
     */
    "manual_review": "manual_review",

    /**
     * value: "moratorium_coverages_not_available"
     * @const
     */
    "moratorium_coverages_not_available": "moratorium_coverages_not_available",

    /**
     * value: "not_enough_data"
     * @const
     */
    "not_enough_data": "not_enough_data",

    /**
     * value: "test_drive"
     * @const
     */
    "test_drive": "test_drive",

    /**
     * value: "vehicle_inspection"
     * @const
     */
    "vehicle_inspection": "vehicle_inspection"
};


/**
 * Allowed values for the <code>actionRequired</code> property.
 * @enum {String}
 * @readonly
 */
UnderwritingRequirement['ActionRequiredEnum'] = {

    /**
     * value: "none"
     * @const
     */
    "none": "none",

    /**
     * value: "retry"
     * @const
     */
    "retry": "retry",

    /**
     * value: "before_bind"
     * @const
     */
    "before_bind": "before_bind",

    /**
     * value: "after_bind"
     * @const
     */
    "after_bind": "after_bind"
};



export default UnderwritingRequirement;

