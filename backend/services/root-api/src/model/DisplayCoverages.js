/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DisplayVehicleCoverages from './DisplayVehicleCoverages';
import PolicyDisplayCoverage from './PolicyDisplayCoverage';

/**
 * The DisplayCoverages model module.
 * @module model/DisplayCoverages
 * @version 3.0.0
 */
class DisplayCoverages {
    /**
     * Constructs a new <code>DisplayCoverages</code>.
     * This object is meant to represent the current coverage selections associated with this Root provided quote.
     * @alias module:model/DisplayCoverages
     * @param policy {Array.<module:model/PolicyDisplayCoverage>} Coverages under this key apply to the whole policy can only have a single value.
     * @param vehicles {Array.<module:model/DisplayVehicleCoverages>} Coverages under this key apply to an individual vehicle and can be selected independently.
     * @param automotive {Array.<module:model/PolicyDisplayCoverage>} (Experimental) A flat array of all coverage selections, as a sibling to policy and vehicles. This is expected to eventually replace the policy/vehicles split. Please see your Root representative to request access to this experimental endpoint.
     */
    constructor(policy, vehicles, automotive) { 
        
        DisplayCoverages.initialize(this, policy, vehicles, automotive);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, policy, vehicles, automotive) { 
        obj['policy'] = policy;
        obj['vehicles'] = vehicles;
        obj['automotive'] = automotive;
    }

    /**
     * Constructs a <code>DisplayCoverages</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DisplayCoverages} obj Optional instance to populate.
     * @return {module:model/DisplayCoverages} The populated <code>DisplayCoverages</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DisplayCoverages();

            if (data.hasOwnProperty('policy')) {
                obj['policy'] = ApiClient.convertToType(data['policy'], [PolicyDisplayCoverage]);
            }
            if (data.hasOwnProperty('vehicles')) {
                obj['vehicles'] = ApiClient.convertToType(data['vehicles'], [DisplayVehicleCoverages]);
            }
            if (data.hasOwnProperty('automotive')) {
                obj['automotive'] = ApiClient.convertToType(data['automotive'], [PolicyDisplayCoverage]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DisplayCoverages</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DisplayCoverages</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DisplayCoverages.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['policy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['policy'])) {
                throw new Error("Expected the field `policy` to be an array in the JSON data but got " + data['policy']);
            }
            // validate the optional field `policy` (array)
            for (const item of data['policy']) {
                PolicyDisplayCoverage.validateJSON(item);
            };
        }
        if (data['vehicles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vehicles'])) {
                throw new Error("Expected the field `vehicles` to be an array in the JSON data but got " + data['vehicles']);
            }
            // validate the optional field `vehicles` (array)
            for (const item of data['vehicles']) {
                DisplayVehicleCoverages.validateJSON(item);
            };
        }
        if (data['automotive']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['automotive'])) {
                throw new Error("Expected the field `automotive` to be an array in the JSON data but got " + data['automotive']);
            }
            // validate the optional field `automotive` (array)
            for (const item of data['automotive']) {
                PolicyDisplayCoverage.validateJSON(item);
            };
        }

        return true;
    }


}

DisplayCoverages.RequiredProperties = ["policy", "vehicles", "automotive"];

/**
 * Coverages under this key apply to the whole policy can only have a single value.
 * @member {Array.<module:model/PolicyDisplayCoverage>} policy
 */
DisplayCoverages.prototype['policy'] = undefined;

/**
 * Coverages under this key apply to an individual vehicle and can be selected independently.
 * @member {Array.<module:model/DisplayVehicleCoverages>} vehicles
 */
DisplayCoverages.prototype['vehicles'] = undefined;

/**
 * (Experimental) A flat array of all coverage selections, as a sibling to policy and vehicles. This is expected to eventually replace the policy/vehicles split. Please see your Root representative to request access to this experimental endpoint.
 * @member {Array.<module:model/PolicyDisplayCoverage>} automotive
 */
DisplayCoverages.prototype['automotive'] = undefined;






export default DisplayCoverages;

