/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApiFactRequirement model module.
 * @module model/ApiFactRequirement
 * @version 3.0.0
 */
class ApiFactRequirement {
    /**
     * Constructs a new <code>ApiFactRequirement</code>.
     * A profile data requirement description
     * @alias module:model/ApiFactRequirement
     * @param fulfilled {Boolean} Whether the requirement has been fulfilled
     * @param kind {module:model/ApiFactRequirement.KindEnum} The kind of requirement
     * @param message {String} An optional message describing the requirement
     */
    constructor(fulfilled, kind, message) { 
        
        ApiFactRequirement.initialize(this, fulfilled, kind, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fulfilled, kind, message) { 
        obj['fulfilled'] = fulfilled;
        obj['kind'] = kind;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>ApiFactRequirement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiFactRequirement} obj Optional instance to populate.
     * @return {module:model/ApiFactRequirement} The populated <code>ApiFactRequirement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiFactRequirement();

            if (data.hasOwnProperty('fulfilled')) {
                obj['fulfilled'] = ApiClient.convertToType(data['fulfilled'], 'Boolean');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiFactRequirement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiFactRequirement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiFactRequirement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }

        return true;
    }


}

ApiFactRequirement.RequiredProperties = ["fulfilled", "kind", "message"];

/**
 * Whether the requirement has been fulfilled
 * @member {Boolean} fulfilled
 */
ApiFactRequirement.prototype['fulfilled'] = undefined;

/**
 * The kind of requirement
 * @member {module:model/ApiFactRequirement.KindEnum} kind
 */
ApiFactRequirement.prototype['kind'] = undefined;

/**
 * An optional message describing the requirement
 * @member {String} message
 */
ApiFactRequirement.prototype['message'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
ApiFactRequirement['KindEnum'] = {

    /**
     * value: "date_range"
     * @const
     */
    "date_range": "date_range",

    /**
     * value: "inclusion"
     * @const
     */
    "inclusion": "inclusion",

    /**
     * value: "incorrect_coverage_scope"
     * @const
     */
    "incorrect_coverage_scope": "incorrect_coverage_scope",

    /**
     * value: "incorrect_format"
     * @const
     */
    "incorrect_format": "incorrect_format",

    /**
     * value: "vin"
     * @const
     */
    "vin": "vin",

    /**
     * value: "ineligible"
     * @const
     */
    "ineligible": "ineligible",

    /**
     * value: "invalid"
     * @const
     */
    "invalid": "invalid",

    /**
     * value: "missing"
     * @const
     */
    "missing": "missing",

    /**
     * value: "no_allowed_value"
     * @const
     */
    "no_allowed_value": "no_allowed_value",

    /**
     * value: "number_range"
     * @const
     */
    "number_range": "number_range",

    /**
     * value: "required_exclusive"
     * @const
     */
    "required_exclusive": "required_exclusive",

    /**
     * value: "required_value"
     * @const
     */
    "required_value": "required_value",

    /**
     * value: "temporarily_unavailable"
     * @const
     */
    "temporarily_unavailable": "temporarily_unavailable",

    /**
     * value: "uniqueness"
     * @const
     */
    "uniqueness": "uniqueness"
};



export default ApiFactRequirement;

