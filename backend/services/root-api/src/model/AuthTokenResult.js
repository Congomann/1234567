/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AuthTokenPayload from './AuthTokenPayload';

/**
 * The AuthTokenResult model module.
 * @module model/AuthTokenResult
 * @version 3.0.0
 */
class AuthTokenResult {
    /**
     * Constructs a new <code>AuthTokenResult</code>.
     * The JWT creation result containing both the encoded JWT and a cleartext version of its contents.
     * @alias module:model/AuthTokenResult
     * @param bearerToken {String} The encoded JWT used as a bearer token to authenticate an agent or agency.
     * @param tokenPayload {module:model/AuthTokenPayload} 
     */
    constructor(bearerToken, tokenPayload) { 
        
        AuthTokenResult.initialize(this, bearerToken, tokenPayload);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bearerToken, tokenPayload) { 
        obj['bearerToken'] = bearerToken;
        obj['tokenPayload'] = tokenPayload;
    }

    /**
     * Constructs a <code>AuthTokenResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthTokenResult} obj Optional instance to populate.
     * @return {module:model/AuthTokenResult} The populated <code>AuthTokenResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthTokenResult();

            if (data.hasOwnProperty('bearerToken')) {
                obj['bearerToken'] = ApiClient.convertToType(data['bearerToken'], 'String');
            }
            if (data.hasOwnProperty('tokenPayload')) {
                obj['tokenPayload'] = AuthTokenPayload.constructFromObject(data['tokenPayload']);
            }
            if (data.hasOwnProperty('partnerAgencyName')) {
                obj['partnerAgencyName'] = ApiClient.convertToType(data['partnerAgencyName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthTokenResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthTokenResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AuthTokenResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bearerToken'] && !(typeof data['bearerToken'] === 'string' || data['bearerToken'] instanceof String)) {
            throw new Error("Expected the field `bearerToken` to be a primitive type in the JSON string but got " + data['bearerToken']);
        }
        // validate the optional field `tokenPayload`
        if (data['tokenPayload']) { // data not null
          AuthTokenPayload.validateJSON(data['tokenPayload']);
        }
        // ensure the json data is a string
        if (data['partnerAgencyName'] && !(typeof data['partnerAgencyName'] === 'string' || data['partnerAgencyName'] instanceof String)) {
            throw new Error("Expected the field `partnerAgencyName` to be a primitive type in the JSON string but got " + data['partnerAgencyName']);
        }

        return true;
    }


}

AuthTokenResult.RequiredProperties = ["bearerToken", "tokenPayload"];

/**
 * The encoded JWT used as a bearer token to authenticate an agent or agency.
 * @member {String} bearerToken
 */
AuthTokenResult.prototype['bearerToken'] = undefined;

/**
 * @member {module:model/AuthTokenPayload} tokenPayload
 */
AuthTokenResult.prototype['tokenPayload'] = undefined;

/**
 * The name of the partner agency associated with the created token.
 * @member {String} partnerAgencyName
 */
AuthTokenResult.prototype['partnerAgencyName'] = undefined;






export default AuthTokenResult;

