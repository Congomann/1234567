/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PolicyPaymentMethod model module.
 * @module model/PolicyPaymentMethod
 * @version 3.0.0
 */
class PolicyPaymentMethod {
    /**
     * Constructs a new <code>PolicyPaymentMethod</code>.
     * The payment method.
     * @alias module:model/PolicyPaymentMethod
     * @param cardType {String} A description of the type of payment method.
     * @param expirationMonth {Number} A expiration month.
     * @param expirationYear {Number} A expiration year.
     * @param last4 {Number} The last 4 digits.
     */
    constructor(cardType, expirationMonth, expirationYear, last4) { 
        
        PolicyPaymentMethod.initialize(this, cardType, expirationMonth, expirationYear, last4);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cardType, expirationMonth, expirationYear, last4) { 
        obj['cardType'] = cardType;
        obj['expirationMonth'] = expirationMonth;
        obj['expirationYear'] = expirationYear;
        obj['last4'] = last4;
    }

    /**
     * Constructs a <code>PolicyPaymentMethod</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PolicyPaymentMethod} obj Optional instance to populate.
     * @return {module:model/PolicyPaymentMethod} The populated <code>PolicyPaymentMethod</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PolicyPaymentMethod();

            if (data.hasOwnProperty('cardType')) {
                obj['cardType'] = ApiClient.convertToType(data['cardType'], 'String');
            }
            if (data.hasOwnProperty('expirationMonth')) {
                obj['expirationMonth'] = ApiClient.convertToType(data['expirationMonth'], 'Number');
            }
            if (data.hasOwnProperty('expirationYear')) {
                obj['expirationYear'] = ApiClient.convertToType(data['expirationYear'], 'Number');
            }
            if (data.hasOwnProperty('last4')) {
                obj['last4'] = ApiClient.convertToType(data['last4'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PolicyPaymentMethod</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PolicyPaymentMethod</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PolicyPaymentMethod.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cardType'] && !(typeof data['cardType'] === 'string' || data['cardType'] instanceof String)) {
            throw new Error("Expected the field `cardType` to be a primitive type in the JSON string but got " + data['cardType']);
        }

        return true;
    }


}

PolicyPaymentMethod.RequiredProperties = ["cardType", "expirationMonth", "expirationYear", "last4"];

/**
 * A description of the type of payment method.
 * @member {String} cardType
 */
PolicyPaymentMethod.prototype['cardType'] = undefined;

/**
 * A expiration month.
 * @member {Number} expirationMonth
 */
PolicyPaymentMethod.prototype['expirationMonth'] = undefined;

/**
 * A expiration year.
 * @member {Number} expirationYear
 */
PolicyPaymentMethod.prototype['expirationYear'] = undefined;

/**
 * The last 4 digits.
 * @member {Number} last4
 */
PolicyPaymentMethod.prototype['last4'] = undefined;






export default PolicyPaymentMethod;

