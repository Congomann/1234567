/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Payment from './Payment';

/**
 * The GetPolicyQuote200ResponseQuote model module.
 * @module model/GetPolicyQuote200ResponseQuote
 * @version 3.0.0
 */
class GetPolicyQuote200ResponseQuote {
    /**
     * Constructs a new <code>GetPolicyQuote200ResponseQuote</code>.
     * Read-only quote payment information for the policy.
     * @alias module:model/GetPolicyQuote200ResponseQuote
     * @param fullTermPayment {module:model/Payment} 
     * @param monthlyTermPayments {Array.<module:model/Payment>} The monthly term payments when the policy is paid monthly.
     */
    constructor(fullTermPayment, monthlyTermPayments) { 
        
        GetPolicyQuote200ResponseQuote.initialize(this, fullTermPayment, monthlyTermPayments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fullTermPayment, monthlyTermPayments) { 
        obj['fullTermPayment'] = fullTermPayment;
        obj['monthlyTermPayments'] = monthlyTermPayments;
    }

    /**
     * Constructs a <code>GetPolicyQuote200ResponseQuote</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetPolicyQuote200ResponseQuote} obj Optional instance to populate.
     * @return {module:model/GetPolicyQuote200ResponseQuote} The populated <code>GetPolicyQuote200ResponseQuote</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetPolicyQuote200ResponseQuote();

            if (data.hasOwnProperty('fullTermPayment')) {
                obj['fullTermPayment'] = Payment.constructFromObject(data['fullTermPayment']);
            }
            if (data.hasOwnProperty('monthlyTermPayments')) {
                obj['monthlyTermPayments'] = ApiClient.convertToType(data['monthlyTermPayments'], [Payment]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetPolicyQuote200ResponseQuote</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetPolicyQuote200ResponseQuote</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GetPolicyQuote200ResponseQuote.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `fullTermPayment`
        if (data['fullTermPayment']) { // data not null
          Payment.validateJSON(data['fullTermPayment']);
        }
        if (data['monthlyTermPayments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['monthlyTermPayments'])) {
                throw new Error("Expected the field `monthlyTermPayments` to be an array in the JSON data but got " + data['monthlyTermPayments']);
            }
            // validate the optional field `monthlyTermPayments` (array)
            for (const item of data['monthlyTermPayments']) {
                Payment.validateJSON(item);
            };
        }

        return true;
    }


}

GetPolicyQuote200ResponseQuote.RequiredProperties = ["fullTermPayment", "monthlyTermPayments"];

/**
 * @member {module:model/Payment} fullTermPayment
 */
GetPolicyQuote200ResponseQuote.prototype['fullTermPayment'] = undefined;

/**
 * The monthly term payments when the policy is paid monthly.
 * @member {Array.<module:model/Payment>} monthlyTermPayments
 */
GetPolicyQuote200ResponseQuote.prototype['monthlyTermPayments'] = undefined;






export default GetPolicyQuote200ResponseQuote;

