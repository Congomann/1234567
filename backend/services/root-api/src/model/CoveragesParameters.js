/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PolicyCoverageParameters from './PolicyCoverageParameters';
import VehicleCoveragesParameters from './VehicleCoveragesParameters';

/**
 * The CoveragesParameters model module.
 * @module model/CoveragesParameters
 * @version 3.0.0
 */
class CoveragesParameters {
    /**
     * Constructs a new <code>CoveragesParameters</code>.
     * This object is meant to represent the current coverage selections associated with this quote.
     * @alias module:model/CoveragesParameters
     */
    constructor() { 
        
        CoveragesParameters.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CoveragesParameters</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CoveragesParameters} obj Optional instance to populate.
     * @return {module:model/CoveragesParameters} The populated <code>CoveragesParameters</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CoveragesParameters();

            if (data.hasOwnProperty('policy')) {
                obj['policy'] = ApiClient.convertToType(data['policy'], [PolicyCoverageParameters]);
            }
            if (data.hasOwnProperty('vehicles')) {
                obj['vehicles'] = ApiClient.convertToType(data['vehicles'], [VehicleCoveragesParameters]);
            }
            if (data.hasOwnProperty('automotive')) {
                obj['automotive'] = ApiClient.convertToType(data['automotive'], [PolicyCoverageParameters]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CoveragesParameters</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CoveragesParameters</code>.
     */
    static validateJSON(data) {
        if (data['policy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['policy'])) {
                throw new Error("Expected the field `policy` to be an array in the JSON data but got " + data['policy']);
            }
            // validate the optional field `policy` (array)
            for (const item of data['policy']) {
                PolicyCoverageParameters.validateJSON(item);
            };
        }
        if (data['vehicles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vehicles'])) {
                throw new Error("Expected the field `vehicles` to be an array in the JSON data but got " + data['vehicles']);
            }
            // validate the optional field `vehicles` (array)
            for (const item of data['vehicles']) {
                VehicleCoveragesParameters.validateJSON(item);
            };
        }
        if (data['automotive']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['automotive'])) {
                throw new Error("Expected the field `automotive` to be an array in the JSON data but got " + data['automotive']);
            }
            // validate the optional field `automotive` (array)
            for (const item of data['automotive']) {
                PolicyCoverageParameters.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Coverages under this key apply to the whole policy.
 * @member {Array.<module:model/PolicyCoverageParameters>} policy
 */
CoveragesParameters.prototype['policy'] = undefined;

/**
 * Coverages under this key apply to an individual vehicle and can be selected independently.
 * @member {Array.<module:model/VehicleCoveragesParameters>} vehicles
 */
CoveragesParameters.prototype['vehicles'] = undefined;

/**
 * (Experimental) A flat array of all coverage selections, as a sibling to policy and vehicles. This is expected to eventually replace the policy/vehicles split. Please see your Root representative to request access to this experimental endpoint.
 * @member {Array.<module:model/PolicyCoverageParameters>} automotive
 */
CoveragesParameters.prototype['automotive'] = undefined;






export default CoveragesParameters;

