/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AuthTokenPayload model module.
 * @module model/AuthTokenPayload
 * @version 3.0.0
 */
class AuthTokenPayload {
    /**
     * Constructs a new <code>AuthTokenPayload</code>.
     * The information contained in the JWT.
     * @alias module:model/AuthTokenPayload
     * @param agentId {String} The ID of the agent that will be attributed to quotes made using this token.
     * @param exp {Object} The unix epoch timestamp when the JWT will expire.
     * @param iat {Number} The unix epoch timestamp when the JWT was issued.
     * @param ref {String} The token reference associated with a partner agency.
     */
    constructor(agentId, exp, iat, ref) { 
        
        AuthTokenPayload.initialize(this, agentId, exp, iat, ref);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, agentId, exp, iat, ref) { 
        obj['agentId'] = agentId;
        obj['exp'] = exp;
        obj['iat'] = iat;
        obj['ref'] = ref;
    }

    /**
     * Constructs a <code>AuthTokenPayload</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthTokenPayload} obj Optional instance to populate.
     * @return {module:model/AuthTokenPayload} The populated <code>AuthTokenPayload</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthTokenPayload();

            if (data.hasOwnProperty('agentId')) {
                obj['agentId'] = ApiClient.convertToType(data['agentId'], 'String');
            }
            if (data.hasOwnProperty('exp')) {
                obj['exp'] = ApiClient.convertToType(data['exp'], Object);
            }
            if (data.hasOwnProperty('iat')) {
                obj['iat'] = ApiClient.convertToType(data['iat'], 'Number');
            }
            if (data.hasOwnProperty('ref')) {
                obj['ref'] = ApiClient.convertToType(data['ref'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthTokenPayload</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthTokenPayload</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AuthTokenPayload.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['agentId'] && !(typeof data['agentId'] === 'string' || data['agentId'] instanceof String)) {
            throw new Error("Expected the field `agentId` to be a primitive type in the JSON string but got " + data['agentId']);
        }
        // ensure the json data is a string
        if (data['ref'] && !(typeof data['ref'] === 'string' || data['ref'] instanceof String)) {
            throw new Error("Expected the field `ref` to be a primitive type in the JSON string but got " + data['ref']);
        }

        return true;
    }


}

AuthTokenPayload.RequiredProperties = ["agentId", "exp", "iat", "ref"];

/**
 * The ID of the agent that will be attributed to quotes made using this token.
 * @member {String} agentId
 */
AuthTokenPayload.prototype['agentId'] = undefined;

/**
 * The unix epoch timestamp when the JWT will expire.
 * @member {Object} exp
 */
AuthTokenPayload.prototype['exp'] = undefined;

/**
 * The unix epoch timestamp when the JWT was issued.
 * @member {Number} iat
 */
AuthTokenPayload.prototype['iat'] = undefined;

/**
 * The token reference associated with a partner agency.
 * @member {String} ref
 */
AuthTokenPayload.prototype['ref'] = undefined;






export default AuthTokenPayload;

