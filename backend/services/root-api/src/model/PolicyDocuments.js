/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PolicyDocuments model module.
 * @module model/PolicyDocuments
 * @version 3.0.0
 */
class PolicyDocuments {
    /**
     * Constructs a new <code>PolicyDocuments</code>.
     * A documents associated with this policy.
     * @alias module:model/PolicyDocuments
     * @param insuranceCardUrl {String} A url the insurance card used for proof of insurance.
     * @param policyDocumentUrl {String} A url the policy document for this policy.
     */
    constructor(insuranceCardUrl, policyDocumentUrl) { 
        
        PolicyDocuments.initialize(this, insuranceCardUrl, policyDocumentUrl);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, insuranceCardUrl, policyDocumentUrl) { 
        obj['insuranceCardUrl'] = insuranceCardUrl;
        obj['policyDocumentUrl'] = policyDocumentUrl;
    }

    /**
     * Constructs a <code>PolicyDocuments</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PolicyDocuments} obj Optional instance to populate.
     * @return {module:model/PolicyDocuments} The populated <code>PolicyDocuments</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PolicyDocuments();

            if (data.hasOwnProperty('insuranceCardUrl')) {
                obj['insuranceCardUrl'] = ApiClient.convertToType(data['insuranceCardUrl'], 'String');
            }
            if (data.hasOwnProperty('policyDocumentUrl')) {
                obj['policyDocumentUrl'] = ApiClient.convertToType(data['policyDocumentUrl'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PolicyDocuments</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PolicyDocuments</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PolicyDocuments.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['insuranceCardUrl'] && !(typeof data['insuranceCardUrl'] === 'string' || data['insuranceCardUrl'] instanceof String)) {
            throw new Error("Expected the field `insuranceCardUrl` to be a primitive type in the JSON string but got " + data['insuranceCardUrl']);
        }
        // ensure the json data is a string
        if (data['policyDocumentUrl'] && !(typeof data['policyDocumentUrl'] === 'string' || data['policyDocumentUrl'] instanceof String)) {
            throw new Error("Expected the field `policyDocumentUrl` to be a primitive type in the JSON string but got " + data['policyDocumentUrl']);
        }

        return true;
    }


}

PolicyDocuments.RequiredProperties = ["insuranceCardUrl", "policyDocumentUrl"];

/**
 * A url the insurance card used for proof of insurance.
 * @member {String} insuranceCardUrl
 */
PolicyDocuments.prototype['insuranceCardUrl'] = undefined;

/**
 * A url the policy document for this policy.
 * @member {String} policyDocumentUrl
 */
PolicyDocuments.prototype['policyDocumentUrl'] = undefined;






export default PolicyDocuments;

