/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AuthTokenReference from './AuthTokenReference';

/**
 * The AuthTokenError model module.
 * @module model/AuthTokenError
 * @version 3.0.0
 */
class AuthTokenError {
    /**
     * Constructs a new <code>AuthTokenError</code>.
     * The error response body.
     * @alias module:model/AuthTokenError
     * @param error {String} The error message.
     */
    constructor(error) { 
        
        AuthTokenError.initialize(this, error);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, error) { 
        obj['error'] = error;
    }

    /**
     * Constructs a <code>AuthTokenError</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthTokenError} obj Optional instance to populate.
     * @return {module:model/AuthTokenError} The populated <code>AuthTokenError</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthTokenError();

            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('tokenReferences')) {
                obj['tokenReferences'] = ApiClient.convertToType(data['tokenReferences'], [AuthTokenReference]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthTokenError</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthTokenError</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AuthTokenError.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        if (data['tokenReferences']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tokenReferences'])) {
                throw new Error("Expected the field `tokenReferences` to be an array in the JSON data but got " + data['tokenReferences']);
            }
            // validate the optional field `tokenReferences` (array)
            for (const item of data['tokenReferences']) {
                AuthTokenReference.validateJSON(item);
            };
        }

        return true;
    }


}

AuthTokenError.RequiredProperties = ["error"];

/**
 * The error message.
 * @member {String} error
 */
AuthTokenError.prototype['error'] = undefined;

/**
 * A list of the possible token references associated with your integration and query parameters.
 * @member {Array.<module:model/AuthTokenReference>} tokenReferences
 */
AuthTokenError.prototype['tokenReferences'] = undefined;






export default AuthTokenError;

