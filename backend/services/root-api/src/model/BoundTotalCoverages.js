/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoundCoverage from './BoundCoverage';
import BoundVehicleCoverages from './BoundVehicleCoverages';

/**
 * The BoundTotalCoverages model module.
 * @module model/BoundTotalCoverages
 * @version 3.0.0
 */
class BoundTotalCoverages {
    /**
     * Constructs a new <code>BoundTotalCoverages</code>.
     * The current coverages bound to this Root policy.
     * @alias module:model/BoundTotalCoverages
     * @param policy {Array.<module:model/BoundCoverage>} Coverages under this key apply to the whole policy.
     * @param vehicles {Array.<module:model/BoundVehicleCoverages>} Coverages under this key apply to an individual vehicle.
     * @param automotive {Array.<module:model/BoundCoverage>} (Experimental) A flat array of all coverage selections, as a sibling to policy and vehicles. This is expected to eventually replace the policy/vehicles split. Please see your Root representative to request access to this experimental endpoint.
     */
    constructor(policy, vehicles, automotive) { 
        
        BoundTotalCoverages.initialize(this, policy, vehicles, automotive);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, policy, vehicles, automotive) { 
        obj['policy'] = policy;
        obj['vehicles'] = vehicles;
        obj['automotive'] = automotive;
    }

    /**
     * Constructs a <code>BoundTotalCoverages</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BoundTotalCoverages} obj Optional instance to populate.
     * @return {module:model/BoundTotalCoverages} The populated <code>BoundTotalCoverages</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BoundTotalCoverages();

            if (data.hasOwnProperty('policy')) {
                obj['policy'] = ApiClient.convertToType(data['policy'], [BoundCoverage]);
            }
            if (data.hasOwnProperty('vehicles')) {
                obj['vehicles'] = ApiClient.convertToType(data['vehicles'], [BoundVehicleCoverages]);
            }
            if (data.hasOwnProperty('automotive')) {
                obj['automotive'] = ApiClient.convertToType(data['automotive'], [BoundCoverage]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BoundTotalCoverages</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BoundTotalCoverages</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BoundTotalCoverages.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['policy']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['policy'])) {
                throw new Error("Expected the field `policy` to be an array in the JSON data but got " + data['policy']);
            }
            // validate the optional field `policy` (array)
            for (const item of data['policy']) {
                BoundCoverage.validateJSON(item);
            };
        }
        if (data['vehicles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vehicles'])) {
                throw new Error("Expected the field `vehicles` to be an array in the JSON data but got " + data['vehicles']);
            }
            // validate the optional field `vehicles` (array)
            for (const item of data['vehicles']) {
                BoundVehicleCoverages.validateJSON(item);
            };
        }
        if (data['automotive']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['automotive'])) {
                throw new Error("Expected the field `automotive` to be an array in the JSON data but got " + data['automotive']);
            }
            // validate the optional field `automotive` (array)
            for (const item of data['automotive']) {
                BoundCoverage.validateJSON(item);
            };
        }

        return true;
    }


}

BoundTotalCoverages.RequiredProperties = ["policy", "vehicles", "automotive"];

/**
 * Coverages under this key apply to the whole policy.
 * @member {Array.<module:model/BoundCoverage>} policy
 */
BoundTotalCoverages.prototype['policy'] = undefined;

/**
 * Coverages under this key apply to an individual vehicle.
 * @member {Array.<module:model/BoundVehicleCoverages>} vehicles
 */
BoundTotalCoverages.prototype['vehicles'] = undefined;

/**
 * (Experimental) A flat array of all coverage selections, as a sibling to policy and vehicles. This is expected to eventually replace the policy/vehicles split. Please see your Root representative to request access to this experimental endpoint.
 * @member {Array.<module:model/BoundCoverage>} automotive
 */
BoundTotalCoverages.prototype['automotive'] = undefined;






export default BoundTotalCoverages;

