/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AffirmationStatement from './AffirmationStatement';
import LegalDocument from './LegalDocument';

/**
 * The Legal model module.
 * @module model/Legal
 * @version 3.0.0
 */
class Legal {
    /**
     * Constructs a new <code>Legal</code>.
     * The legal documents along with the affirmation statement.
     * @alias module:model/Legal
     * @param documents {Array.<module:model/LegalDocument>} The array of legal documents.
     * @param affirmationStatement {module:model/AffirmationStatement} 
     */
    constructor(documents, affirmationStatement) { 
        
        Legal.initialize(this, documents, affirmationStatement);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, documents, affirmationStatement) { 
        obj['documents'] = documents;
        obj['affirmationStatement'] = affirmationStatement;
    }

    /**
     * Constructs a <code>Legal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Legal} obj Optional instance to populate.
     * @return {module:model/Legal} The populated <code>Legal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Legal();

            if (data.hasOwnProperty('documents')) {
                obj['documents'] = ApiClient.convertToType(data['documents'], [LegalDocument]);
            }
            if (data.hasOwnProperty('affirmationStatement')) {
                obj['affirmationStatement'] = AffirmationStatement.constructFromObject(data['affirmationStatement']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Legal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Legal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Legal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['documents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['documents'])) {
                throw new Error("Expected the field `documents` to be an array in the JSON data but got " + data['documents']);
            }
            // validate the optional field `documents` (array)
            for (const item of data['documents']) {
                LegalDocument.validateJSON(item);
            };
        }
        // validate the optional field `affirmationStatement`
        if (data['affirmationStatement']) { // data not null
          AffirmationStatement.validateJSON(data['affirmationStatement']);
        }

        return true;
    }


}

Legal.RequiredProperties = ["documents", "affirmationStatement"];

/**
 * The array of legal documents.
 * @member {Array.<module:model/LegalDocument>} documents
 */
Legal.prototype['documents'] = undefined;

/**
 * @member {module:model/AffirmationStatement} affirmationStatement
 */
Legal.prototype['affirmationStatement'] = undefined;






export default Legal;

