/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ResponseTelematicsProgram from './ResponseTelematicsProgram';

/**
 * The OptionsTelematics model module.
 * @module model/OptionsTelematics
 * @version 3.0.0
 */
class OptionsTelematics {
    /**
     * Constructs a new <code>OptionsTelematics</code>.
     * The information regarding a telematics program and its enrollment status.
     * @alias module:model/OptionsTelematics
     */
    constructor() { 
        
        OptionsTelematics.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OptionsTelematics</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OptionsTelematics} obj Optional instance to populate.
     * @return {module:model/OptionsTelematics} The populated <code>OptionsTelematics</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OptionsTelematics();

            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('programs')) {
                obj['programs'] = ApiClient.convertToType(data['programs'], [ResponseTelematicsProgram]);
            }
            if (data.hasOwnProperty('testDrives')) {
                obj['testDrives'] = ApiClient.convertToType(data['testDrives'], [ResponseTelematicsProgram]);
            }
            if (data.hasOwnProperty('electives')) {
                obj['electives'] = ApiClient.convertToType(data['electives'], [ResponseTelematicsProgram]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OptionsTelematics</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OptionsTelematics</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        if (data['programs']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['programs'])) {
                throw new Error("Expected the field `programs` to be an array in the JSON data but got " + data['programs']);
            }
            // validate the optional field `programs` (array)
            for (const item of data['programs']) {
                ResponseTelematicsProgram.validateJSON(item);
            };
        }
        if (data['testDrives']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['testDrives'])) {
                throw new Error("Expected the field `testDrives` to be an array in the JSON data but got " + data['testDrives']);
            }
            // validate the optional field `testDrives` (array)
            for (const item of data['testDrives']) {
                ResponseTelematicsProgram.validateJSON(item);
            };
        }
        if (data['electives']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['electives'])) {
                throw new Error("Expected the field `electives` to be an array in the JSON data but got " + data['electives']);
            }
            // validate the optional field `electives` (array)
            for (const item of data['electives']) {
                ResponseTelematicsProgram.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * The availability of the programs.
 * @member {module:model/OptionsTelematics.StatusEnum} status
 */
OptionsTelematics.prototype['status'] = undefined;

/**
 * DEPRECATED: Use testDrives and electives instead. This field will be removed in an upcoming release.
 * @member {Array.<module:model/ResponseTelematicsProgram>} programs
 */
OptionsTelematics.prototype['programs'] = undefined;

/**
 * The primary telematics programs available to the user. One must be chosen.
 * @member {Array.<module:model/ResponseTelematicsProgram>} testDrives
 */
OptionsTelematics.prototype['testDrives'] = undefined;

/**
 * The secondary telematics programs available to the user. These are optional.
 * @member {Array.<module:model/ResponseTelematicsProgram>} electives
 */
OptionsTelematics.prototype['electives'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
OptionsTelematics['StatusEnum'] = {

    /**
     * value: "available"
     * @const
     */
    "available": "available",

    /**
     * value: "checking_availability"
     * @const
     */
    "checking_availability": "checking_availability",

    /**
     * value: "insufficient_data"
     * @const
     */
    "insufficient_data": "insufficient_data",

    /**
     * value: "unavailable"
     * @const
     */
    "unavailable": "unavailable"
};



export default OptionsTelematics;

