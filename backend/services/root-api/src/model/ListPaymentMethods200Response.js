/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PaymentMethod from './PaymentMethod';

/**
 * The ListPaymentMethods200Response model module.
 * @module model/ListPaymentMethods200Response
 * @version 3.0.0
 */
class ListPaymentMethods200Response {
    /**
     * Constructs a new <code>ListPaymentMethods200Response</code>.
     * The success response body.
     * @alias module:model/ListPaymentMethods200Response
     * @param methods {Array.<module:model/PaymentMethod>} The list of submitted payment methods.
     * @param selectedPaymentMethodId {String} The selected payment method ID, if one exists.
     */
    constructor(methods, selectedPaymentMethodId) { 
        
        ListPaymentMethods200Response.initialize(this, methods, selectedPaymentMethodId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, methods, selectedPaymentMethodId) { 
        obj['methods'] = methods;
        obj['selectedPaymentMethodId'] = selectedPaymentMethodId;
    }

    /**
     * Constructs a <code>ListPaymentMethods200Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ListPaymentMethods200Response} obj Optional instance to populate.
     * @return {module:model/ListPaymentMethods200Response} The populated <code>ListPaymentMethods200Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ListPaymentMethods200Response();

            if (data.hasOwnProperty('methods')) {
                obj['methods'] = ApiClient.convertToType(data['methods'], [PaymentMethod]);
            }
            if (data.hasOwnProperty('selectedPaymentMethodId')) {
                obj['selectedPaymentMethodId'] = ApiClient.convertToType(data['selectedPaymentMethodId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ListPaymentMethods200Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ListPaymentMethods200Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ListPaymentMethods200Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['methods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['methods'])) {
                throw new Error("Expected the field `methods` to be an array in the JSON data but got " + data['methods']);
            }
            // validate the optional field `methods` (array)
            for (const item of data['methods']) {
                PaymentMethod.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['selectedPaymentMethodId'] && !(typeof data['selectedPaymentMethodId'] === 'string' || data['selectedPaymentMethodId'] instanceof String)) {
            throw new Error("Expected the field `selectedPaymentMethodId` to be a primitive type in the JSON string but got " + data['selectedPaymentMethodId']);
        }

        return true;
    }


}

ListPaymentMethods200Response.RequiredProperties = ["methods", "selectedPaymentMethodId"];

/**
 * The list of submitted payment methods.
 * @member {Array.<module:model/PaymentMethod>} methods
 */
ListPaymentMethods200Response.prototype['methods'] = undefined;

/**
 * The selected payment method ID, if one exists.
 * @member {String} selectedPaymentMethodId
 */
ListPaymentMethods200Response.prototype['selectedPaymentMethodId'] = undefined;






export default ListPaymentMethods200Response;

