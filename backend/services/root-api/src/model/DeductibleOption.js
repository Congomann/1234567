/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DeductibleOption model module.
 * @module model/DeductibleOption
 * @version 3.0.0
 */
class DeductibleOption {
    /**
     * Constructs a new <code>DeductibleOption</code>.
     * An attribute representing the deductible level of this coverage.
     * @alias module:model/DeductibleOption
     * @param deductible {Number} The deductible in USD dollars.
     * @param declined {Boolean} (Experimental) Whether or not this deductible is declined. A coverage can have additional deductibles that can be declined independently. Please see your Root representative to request access to this experimental endpoint.
     * @param description {String} A human readable description of the deductible amount.
     * @param longDescription {String} (Experimental) A more detailed human readable description of the deductible amount. Please see your Root representative to request access to this experimental endpoint.
     */
    constructor(deductible, declined, description, longDescription) { 
        
        DeductibleOption.initialize(this, deductible, declined, description, longDescription);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, deductible, declined, description, longDescription) { 
        obj['deductible'] = deductible;
        obj['declined'] = declined;
        obj['description'] = description;
        obj['longDescription'] = longDescription;
    }

    /**
     * Constructs a <code>DeductibleOption</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeductibleOption} obj Optional instance to populate.
     * @return {module:model/DeductibleOption} The populated <code>DeductibleOption</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeductibleOption();

            if (data.hasOwnProperty('deductible')) {
                obj['deductible'] = ApiClient.convertToType(data['deductible'], 'Number');
            }
            if (data.hasOwnProperty('declined')) {
                obj['declined'] = ApiClient.convertToType(data['declined'], 'Boolean');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('longDescription')) {
                obj['longDescription'] = ApiClient.convertToType(data['longDescription'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeductibleOption</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeductibleOption</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeductibleOption.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['longDescription'] && !(typeof data['longDescription'] === 'string' || data['longDescription'] instanceof String)) {
            throw new Error("Expected the field `longDescription` to be a primitive type in the JSON string but got " + data['longDescription']);
        }

        return true;
    }


}

DeductibleOption.RequiredProperties = ["deductible", "declined", "description", "longDescription"];

/**
 * The deductible in USD dollars.
 * @member {Number} deductible
 */
DeductibleOption.prototype['deductible'] = undefined;

/**
 * (Experimental) Whether or not this deductible is declined. A coverage can have additional deductibles that can be declined independently. Please see your Root representative to request access to this experimental endpoint.
 * @member {Boolean} declined
 */
DeductibleOption.prototype['declined'] = undefined;

/**
 * A human readable description of the deductible amount.
 * @member {String} description
 */
DeductibleOption.prototype['description'] = undefined;

/**
 * (Experimental) A more detailed human readable description of the deductible amount. Please see your Root representative to request access to this experimental endpoint.
 * @member {String} longDescription
 */
DeductibleOption.prototype['longDescription'] = undefined;






export default DeductibleOption;

