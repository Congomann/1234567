/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PrefillDriver from './PrefillDriver';
import PrefillVehicle from './PrefillVehicle';

/**
 * The PrefillReport model module.
 * @module model/PrefillReport
 * @version 3.0.0
 */
class PrefillReport {
    /**
     * Constructs a new <code>PrefillReport</code>.
     * A report containing the found drivers and vehicles from the request.
     * @alias module:model/PrefillReport
     * @param drivers {Array.<module:model/PrefillDriver>} Drivers found in this report.
     * @param vehicles {Array.<module:model/PrefillVehicle>} Vehicles found in this report.
     */
    constructor(drivers, vehicles) { 
        
        PrefillReport.initialize(this, drivers, vehicles);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, drivers, vehicles) { 
        obj['drivers'] = drivers;
        obj['vehicles'] = vehicles;
    }

    /**
     * Constructs a <code>PrefillReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrefillReport} obj Optional instance to populate.
     * @return {module:model/PrefillReport} The populated <code>PrefillReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrefillReport();

            if (data.hasOwnProperty('drivers')) {
                obj['drivers'] = ApiClient.convertToType(data['drivers'], [PrefillDriver]);
            }
            if (data.hasOwnProperty('vehicles')) {
                obj['vehicles'] = ApiClient.convertToType(data['vehicles'], [PrefillVehicle]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrefillReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrefillReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrefillReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['drivers']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['drivers'])) {
                throw new Error("Expected the field `drivers` to be an array in the JSON data but got " + data['drivers']);
            }
            // validate the optional field `drivers` (array)
            for (const item of data['drivers']) {
                PrefillDriver.validateJSON(item);
            };
        }
        if (data['vehicles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['vehicles'])) {
                throw new Error("Expected the field `vehicles` to be an array in the JSON data but got " + data['vehicles']);
            }
            // validate the optional field `vehicles` (array)
            for (const item of data['vehicles']) {
                PrefillVehicle.validateJSON(item);
            };
        }

        return true;
    }


}

PrefillReport.RequiredProperties = ["drivers", "vehicles"];

/**
 * Drivers found in this report.
 * @member {Array.<module:model/PrefillDriver>} drivers
 */
PrefillReport.prototype['drivers'] = undefined;

/**
 * Vehicles found in this report.
 * @member {Array.<module:model/PrefillVehicle>} vehicles
 */
PrefillReport.prototype['vehicles'] = undefined;






export default PrefillReport;

