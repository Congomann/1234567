/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BridgeLink model module.
 * @module model/BridgeLink
 * @version 3.0.0
 */
class BridgeLink {
    /**
     * Constructs a new <code>BridgeLink</code>.
     * Information needed to bridge to finish quoting &amp; binding on Root properties. This includes the full URL as well as the token included in the URL.
     * @alias module:model/BridgeLink
     * @param bridgeId {String} The unique identifier for this bridge link.
     * @param expiresAt {Date} When the authentication token expires.
     * @param url {String} The URL to redirect the user to.
     * @param token {String} The time-limited token already included in the URL to authenticate the user to the quote.
     */
    constructor(bridgeId, expiresAt, url, token) { 
        
        BridgeLink.initialize(this, bridgeId, expiresAt, url, token);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bridgeId, expiresAt, url, token) { 
        obj['bridgeId'] = bridgeId;
        obj['expiresAt'] = expiresAt;
        obj['url'] = url;
        obj['token'] = token;
    }

    /**
     * Constructs a <code>BridgeLink</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BridgeLink} obj Optional instance to populate.
     * @return {module:model/BridgeLink} The populated <code>BridgeLink</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BridgeLink();

            if (data.hasOwnProperty('bridgeId')) {
                obj['bridgeId'] = ApiClient.convertToType(data['bridgeId'], 'String');
            }
            if (data.hasOwnProperty('experience')) {
                obj['experience'] = ApiClient.convertToType(data['experience'], 'String');
            }
            if (data.hasOwnProperty('expiresAt')) {
                obj['expiresAt'] = ApiClient.convertToType(data['expiresAt'], 'Date');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BridgeLink</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BridgeLink</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BridgeLink.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bridgeId'] && !(typeof data['bridgeId'] === 'string' || data['bridgeId'] instanceof String)) {
            throw new Error("Expected the field `bridgeId` to be a primitive type in the JSON string but got " + data['bridgeId']);
        }
        // ensure the json data is a string
        if (data['experience'] && !(typeof data['experience'] === 'string' || data['experience'] instanceof String)) {
            throw new Error("Expected the field `experience` to be a primitive type in the JSON string but got " + data['experience']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }

        return true;
    }


}

BridgeLink.RequiredProperties = ["bridgeId", "expiresAt", "url", "token"];

/**
 * The unique identifier for this bridge link.
 * @member {String} bridgeId
 */
BridgeLink.prototype['bridgeId'] = undefined;

/**
 * The experience the bridging user will be directed to.
 * @member {module:model/BridgeLink.ExperienceEnum} experience
 */
BridgeLink.prototype['experience'] = undefined;

/**
 * When the authentication token expires.
 * @member {Date} expiresAt
 */
BridgeLink.prototype['expiresAt'] = undefined;

/**
 * The URL to redirect the user to.
 * @member {String} url
 */
BridgeLink.prototype['url'] = undefined;

/**
 * The time-limited token already included in the URL to authenticate the user to the quote.
 * @member {String} token
 */
BridgeLink.prototype['token'] = undefined;





/**
 * Allowed values for the <code>experience</code> property.
 * @enum {String}
 * @readonly
 */
BridgeLink['ExperienceEnum'] = {

    /**
     * value: "agent"
     * @const
     */
    "agent": "agent",

    /**
     * value: "default"
     * @const
     */
    "default": "default"
};



export default BridgeLink;

