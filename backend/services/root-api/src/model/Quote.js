/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Agent from './Agent';
import Coverages from './Coverages';
import Options from './Options';
import Payment from './Payment';
import Profile from './Profile';
import QuoteRateCall from './QuoteRateCall';

/**
 * The Quote model module.
 * @module model/Quote
 * @version 3.0.0
 */
class Quote {
    /**
     * Constructs a new <code>Quote</code>.
     * The full description of the quote. This includes profile data used for the quote, coverage selections, payment information, and relevant status fields.
     * @alias module:model/Quote
     * @param id {String} The unique identifiers for this quote.
     * @param agent {module:model/Agent} 
     * @param profile {module:model/Profile} 
     * @param kind {module:model/Quote.KindEnum} The kind of quote currently being presented.
     * @param invoicePeriod {module:model/Quote.InvoicePeriodEnum} The invoice period for this quote. This determines the frequency at which the policy holder is billed.
     * @param status {module:model/Quote.StatusEnum} The status of any background actions Root is currently performing in regards to this quote.
     * @param quoteReason {module:model/Quote.QuoteReasonEnum} The reason this quote was created.
     * @param coverages {module:model/Coverages} 
     */
    constructor(id, agent, profile, kind, invoicePeriod, status, quoteReason, coverages) { 
        
        Quote.initialize(this, id, agent, profile, kind, invoicePeriod, status, quoteReason, coverages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, agent, profile, kind, invoicePeriod, status, quoteReason, coverages) { 
        obj['id'] = id;
        obj['agent'] = agent;
        obj['profile'] = profile;
        obj['kind'] = kind;
        obj['invoicePeriod'] = invoicePeriod;
        obj['status'] = status;
        obj['quoteReason'] = quoteReason;
        obj['coverages'] = coverages;
    }

    /**
     * Constructs a <code>Quote</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Quote} obj Optional instance to populate.
     * @return {module:model/Quote} The populated <code>Quote</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Quote();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('agent')) {
                obj['agent'] = Agent.constructFromObject(data['agent']);
            }
            if (data.hasOwnProperty('finalizable')) {
                obj['finalizable'] = ApiClient.convertToType(data['finalizable'], 'Boolean');
            }
            if (data.hasOwnProperty('profile')) {
                obj['profile'] = Profile.constructFromObject(data['profile']);
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = Options.constructFromObject(data['options']);
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('invoicePeriod')) {
                obj['invoicePeriod'] = ApiClient.convertToType(data['invoicePeriod'], 'String');
            }
            if (data.hasOwnProperty('expiresOnOrBefore')) {
                obj['expiresOnOrBefore'] = ApiClient.convertToType(data['expiresOnOrBefore'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('quoteReason')) {
                obj['quoteReason'] = ApiClient.convertToType(data['quoteReason'], 'String');
            }
            if (data.hasOwnProperty('fullTermPayment')) {
                obj['fullTermPayment'] = Payment.constructFromObject(data['fullTermPayment']);
            }
            if (data.hasOwnProperty('monthlyTermPayments')) {
                obj['monthlyTermPayments'] = ApiClient.convertToType(data['monthlyTermPayments'], [Payment]);
            }
            if (data.hasOwnProperty('coverages')) {
                obj['coverages'] = Coverages.constructFromObject(data['coverages']);
            }
            if (data.hasOwnProperty('originatingPolicyNumber')) {
                obj['originatingPolicyNumber'] = ApiClient.convertToType(data['originatingPolicyNumber'], 'String');
            }
            if (data.hasOwnProperty('boundPolicyNumber')) {
                obj['boundPolicyNumber'] = ApiClient.convertToType(data['boundPolicyNumber'], 'String');
            }
            if (data.hasOwnProperty('rateCallId')) {
                obj['rateCallId'] = ApiClient.convertToType(data['rateCallId'], 'String');
            }
            if (data.hasOwnProperty('rateCall')) {
                obj['rateCall'] = QuoteRateCall.constructFromObject(data['rateCall']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Quote</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Quote</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Quote.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // validate the optional field `agent`
        if (data['agent']) { // data not null
          Agent.validateJSON(data['agent']);
        }
        // validate the optional field `profile`
        if (data['profile']) { // data not null
          Profile.validateJSON(data['profile']);
        }
        // validate the optional field `options`
        if (data['options']) { // data not null
          Options.validateJSON(data['options']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['invoicePeriod'] && !(typeof data['invoicePeriod'] === 'string' || data['invoicePeriod'] instanceof String)) {
            throw new Error("Expected the field `invoicePeriod` to be a primitive type in the JSON string but got " + data['invoicePeriod']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['quoteReason'] && !(typeof data['quoteReason'] === 'string' || data['quoteReason'] instanceof String)) {
            throw new Error("Expected the field `quoteReason` to be a primitive type in the JSON string but got " + data['quoteReason']);
        }
        // validate the optional field `fullTermPayment`
        if (data['fullTermPayment']) { // data not null
          Payment.validateJSON(data['fullTermPayment']);
        }
        if (data['monthlyTermPayments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['monthlyTermPayments'])) {
                throw new Error("Expected the field `monthlyTermPayments` to be an array in the JSON data but got " + data['monthlyTermPayments']);
            }
            // validate the optional field `monthlyTermPayments` (array)
            for (const item of data['monthlyTermPayments']) {
                Payment.validateJSON(item);
            };
        }
        // validate the optional field `coverages`
        if (data['coverages']) { // data not null
          Coverages.validateJSON(data['coverages']);
        }
        // ensure the json data is a string
        if (data['originatingPolicyNumber'] && !(typeof data['originatingPolicyNumber'] === 'string' || data['originatingPolicyNumber'] instanceof String)) {
            throw new Error("Expected the field `originatingPolicyNumber` to be a primitive type in the JSON string but got " + data['originatingPolicyNumber']);
        }
        // ensure the json data is a string
        if (data['boundPolicyNumber'] && !(typeof data['boundPolicyNumber'] === 'string' || data['boundPolicyNumber'] instanceof String)) {
            throw new Error("Expected the field `boundPolicyNumber` to be a primitive type in the JSON string but got " + data['boundPolicyNumber']);
        }
        // ensure the json data is a string
        if (data['rateCallId'] && !(typeof data['rateCallId'] === 'string' || data['rateCallId'] instanceof String)) {
            throw new Error("Expected the field `rateCallId` to be a primitive type in the JSON string but got " + data['rateCallId']);
        }
        // validate the optional field `rateCall`
        if (data['rateCall']) { // data not null
          QuoteRateCall.validateJSON(data['rateCall']);
        }

        return true;
    }


}

Quote.RequiredProperties = ["id", "agent", "profile", "kind", "invoicePeriod", "status", "quoteReason", "coverages"];

/**
 * The unique identifiers for this quote.
 * @member {String} id
 */
Quote.prototype['id'] = undefined;

/**
 * @member {module:model/Agent} agent
 */
Quote.prototype['agent'] = undefined;

/**
 * Whether or not this quote is ready to be finalized.
 * @member {Boolean} finalizable
 */
Quote.prototype['finalizable'] = undefined;

/**
 * @member {module:model/Profile} profile
 */
Quote.prototype['profile'] = undefined;

/**
 * @member {module:model/Options} options
 */
Quote.prototype['options'] = undefined;

/**
 * The kind of quote currently being presented.
 * @member {module:model/Quote.KindEnum} kind
 */
Quote.prototype['kind'] = undefined;

/**
 * The invoice period for this quote. This determines the frequency at which the policy holder is billed.
 * @member {module:model/Quote.InvoicePeriodEnum} invoicePeriod
 */
Quote.prototype['invoicePeriod'] = undefined;

/**
 * The latest date and time at which this quote will expire. Quotes can expire sooner, but this is the maximum time it could be valid for.
 * @member {Date} expiresOnOrBefore
 */
Quote.prototype['expiresOnOrBefore'] = undefined;

/**
 * The status of any background actions Root is currently performing in regards to this quote.
 * @member {module:model/Quote.StatusEnum} status
 */
Quote.prototype['status'] = undefined;

/**
 * The reason this quote was created.
 * @member {module:model/Quote.QuoteReasonEnum} quoteReason
 */
Quote.prototype['quoteReason'] = undefined;

/**
 * @member {module:model/Payment} fullTermPayment
 */
Quote.prototype['fullTermPayment'] = undefined;

/**
 * The payments required if the customer pays for the 6-month policy monthly.
 * @member {Array.<module:model/Payment>} monthlyTermPayments
 */
Quote.prototype['monthlyTermPayments'] = undefined;

/**
 * @member {module:model/Coverages} coverages
 */
Quote.prototype['coverages'] = undefined;

/**
 * The policy number of the policy that this quote originated from, if applicable.
 * @member {String} originatingPolicyNumber
 */
Quote.prototype['originatingPolicyNumber'] = undefined;

/**
 * The policy number of the bound policy for this quote, if applicable.
 * @member {String} boundPolicyNumber
 */
Quote.prototype['boundPolicyNumber'] = undefined;

/**
 * A reference to the last created rate call, a unique indicator of a priced quote. This value is used to reference the created price/rate call as the quote is updated.
 * @member {String} rateCallId
 */
Quote.prototype['rateCallId'] = undefined;

/**
 * @member {module:model/QuoteRateCall} rateCall
 */
Quote.prototype['rateCall'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
Quote['KindEnum'] = {

    /**
     * value: "none"
     * @const
     */
    "none": "none",

    /**
     * value: "estimated_quote"
     * @const
     */
    "estimated_quote": "estimated_quote",

    /**
     * value: "bindable_quote"
     * @const
     */
    "bindable_quote": "bindable_quote",

    /**
     * value: "finalized_quote"
     * @const
     */
    "finalized_quote": "finalized_quote"
};


/**
 * Allowed values for the <code>invoicePeriod</code> property.
 * @enum {String}
 * @readonly
 */
Quote['InvoicePeriodEnum'] = {

    /**
     * value: "monthly"
     * @const
     */
    "monthly": "monthly",

    /**
     * value: "full_term"
     * @const
     */
    "full_term": "full_term"
};


/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
Quote['StatusEnum'] = {

    /**
     * value: "collecting_data"
     * @const
     */
    "collecting_data": "collecting_data",

    /**
     * value: "quoted"
     * @const
     */
    "quoted": "quoted",

    /**
     * value: "quoting"
     * @const
     */
    "quoting": "quoting",

    /**
     * value: "errored"
     * @const
     */
    "errored": "errored",

    /**
     * value: "estimate_not_available"
     * @const
     */
    "estimate_not_available": "estimate_not_available",

    /**
     * value: "requirements_not_met"
     * @const
     */
    "requirements_not_met": "requirements_not_met",

    /**
     * value: "underwriting_declined"
     * @const
     */
    "underwriting_declined": "underwriting_declined",

    /**
     * value: "underwriting_pending"
     * @const
     */
    "underwriting_pending": "underwriting_pending"
};


/**
 * Allowed values for the <code>quoteReason</code> property.
 * @enum {String}
 * @readonly
 */
Quote['QuoteReasonEnum'] = {

    /**
     * value: "endorsement"
     * @const
     */
    "endorsement": "endorsement",

    /**
     * value: "new_business"
     * @const
     */
    "new_business": "new_business"
};



export default Quote;

