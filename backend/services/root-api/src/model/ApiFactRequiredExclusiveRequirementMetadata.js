/**
 * Root Embedded API
 * APIs available to Root embedded partners
 *
 * The version of the OpenAPI document: 3.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApiFactRequiredExclusiveRequirementMetadata model module.
 * @module model/ApiFactRequiredExclusiveRequirementMetadata
 * @version 3.0.0
 */
class ApiFactRequiredExclusiveRequirementMetadata {
    /**
     * Constructs a new <code>ApiFactRequiredExclusiveRequirementMetadata</code>.
     * Metadata to describe values that must each appear on exactly one entity
     * @alias module:model/ApiFactRequiredExclusiveRequirementMetadata
     * @param exclusiveValues {Array.<String>} Values that must each be present on exactly one entity
     * @param set {Array.<String>} The set of values currently present across entities
     */
    constructor(exclusiveValues, set) { 
        
        ApiFactRequiredExclusiveRequirementMetadata.initialize(this, exclusiveValues, set);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, exclusiveValues, set) { 
        obj['exclusiveValues'] = exclusiveValues;
        obj['set'] = set;
    }

    /**
     * Constructs a <code>ApiFactRequiredExclusiveRequirementMetadata</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiFactRequiredExclusiveRequirementMetadata} obj Optional instance to populate.
     * @return {module:model/ApiFactRequiredExclusiveRequirementMetadata} The populated <code>ApiFactRequiredExclusiveRequirementMetadata</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiFactRequiredExclusiveRequirementMetadata();

            if (data.hasOwnProperty('exclusiveValues')) {
                obj['exclusiveValues'] = ApiClient.convertToType(data['exclusiveValues'], ['String']);
            }
            if (data.hasOwnProperty('set')) {
                obj['set'] = ApiClient.convertToType(data['set'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiFactRequiredExclusiveRequirementMetadata</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiFactRequiredExclusiveRequirementMetadata</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiFactRequiredExclusiveRequirementMetadata.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['exclusiveValues'])) {
            throw new Error("Expected the field `exclusiveValues` to be an array in the JSON data but got " + data['exclusiveValues']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['set'])) {
            throw new Error("Expected the field `set` to be an array in the JSON data but got " + data['set']);
        }

        return true;
    }


}

ApiFactRequiredExclusiveRequirementMetadata.RequiredProperties = ["exclusiveValues", "set"];

/**
 * Values that must each be present on exactly one entity
 * @member {Array.<String>} exclusiveValues
 */
ApiFactRequiredExclusiveRequirementMetadata.prototype['exclusiveValues'] = undefined;

/**
 * The set of values currently present across entities
 * @member {Array.<String>} set
 */
ApiFactRequiredExclusiveRequirementMetadata.prototype['set'] = undefined;






export default ApiFactRequiredExclusiveRequirementMetadata;

